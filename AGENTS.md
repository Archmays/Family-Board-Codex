# AGENTS.md

- 这是本地可编辑、GitHub Pages 完全只读的家庭信息板。
- 日常内容只通过本地 Editor 修改唯一数据源 `data/board.json`；`docs/` 是生成的发布快照，不手改。
- 不加入数据库、账号、云后端或 Pages 在线编辑能力。
- 不提交敏感数据；公开页面与发布照片都按公开互联网资源处理。
- 临时截图、trace、video 只放项目 `.tmp/` 下本次任务专用文件夹；任务结束时提供完整路径供用户手动删除，不自动清理。
